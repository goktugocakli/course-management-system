import { AccountsContainer, NavContainer } from "../../containers";

export default function AccountsPage({ signin }) {
  return (
    <>
      <NavContainer />
      <AccountsContainer />
    </>
  );
}
